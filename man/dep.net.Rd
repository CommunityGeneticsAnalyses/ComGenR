% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dep.net.R
\name{dep.net}
\alias{dep.net}
\title{Generates the dependency network from Araujo et al. 2011.}
\usage{
dep.net(x = "species in cols", zero.na = TRUE, prune = TRUE, diag.zero = TRUE)
}
\arguments{
\item{x}{A matrix of species abundances with species in columns.}

\item{zero.na}{Automatically make NA values zero.}

\item{prune}{Reduce the size of the network using a confidence interval
test.}

\item{diag.zero}{Set the matrix diagonal (i.e. the trace) to zero.}
}
\value{
%% ~Describe the value returned %% If it is a LIST, use %%
}
\description{
Models and conducts network pruning for a species dependency network
calculated as pairwise conditional probabilities.
}
\details{
%% ~~ If necessary, more details than the description above ~~
}
\note{
%% ~~further notes~~
}
\examples{

##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (x = "species in cols", zero.na = TRUE, prune = TRUE, 
    diag.zero = TRUE, pos = TRUE) 
{
    out <- matrix(NA, nrow = ncol(x), ncol = ncol(x))
    for (i in 1:ncol(x)) {
        for (j in 1:ncol(x)) {
            if (pos) {
                out[i, j] <- calcDepend(x[, i], x[, j])
            }
            else {
                out[i, j] <- negDepend(x[, i], x[, j])
            }
        }
    }
    if (prune) {
        out.rm <- co.net(x, diag.zero = diag.zero)
        out[out.rm == 0] <- 0
    }
    else {
    }
    if (diag.zero) {
        diag(out) <- 0
    }
    rownames(out) <- colnames(out) <- colnames(x)
    if (zero.na) {
        out[is.na(out)] <- 0
    }
    return(out)
  }

}
\references{
%% ~put references to the literature/web site here ~
}
\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\author{
Matthew K. Lau
}
\keyword{~kwd1}
\keyword{~kwd2}
